const authConfig = {
    providers: [
        {
            domain: "https://concrete-raptor-42.clerk.accounts.dev",
            applicationID: "convex",
        },
    ],
};

export default authConfig;
